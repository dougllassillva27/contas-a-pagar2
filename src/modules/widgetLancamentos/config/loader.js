const path=require('path');const fs=require('fs');const CONFIG_PATH=path.join(__dirname,'default.json');const DEFAULTS={apiUrl:'http://localhost:3000',apiToken:'',hotkey:'Ctrl+Alt+N',defaultUserId:1,autoStart:false,timeout:10000};function loadConfig(){let userConfig={};try{if(fs.existsSync(CONFIG_PATH)){const raw=fs.readFileSync(CONFIG_PATH,'utf8');userConfig=JSON.parse(raw);}}catch(err){console.error('[Config] Erro:',err.message);}const config={...DEFAULTS,...userConfig};if(!config.apiUrl||!config.apiUrl.startsWith('http')){console.warn('[Config] apiUrl inválida');config.apiUrl=DEFAULTS.apiUrl;}if(!config.apiToken||config.apiToken==='SEU_API_TOKEN_AQUI'){console.warn('[Config] apiToken não configurado');}return config;}function saveConfig(updates){try{const current=loadConfig();const merged={...current,...updates};if(merged.apiToken===''||merged.apiToken==='SEU_API_TOKEN_AQUI')delete merged.apiToken;fs.writeFileSync(CONFIG_PATH,JSON.stringify(merged,null,2),'utf8');return true;}catch(err){console.error('[Config] Erro ao salvar:',err.message);return false;}}module.exports={loadConfig,saveConfig,getConfigPath:()=>CONFIG_PATH,DEFAULTS};