(function(){'use strict';const form=document.getElementById('lancamentoForm');const btnClose=document.getElementById('btnClose');const btnCancel=document.getElementById('btnCancel');const btnSubmit=document.getElementById('btnSubmit');const loading=document.getElementById('loading');const statusMessage=document.getElementById('statusMessage');const usuarioInput=document.getElementById('usuario_id');const btnDodo=document.getElementById('btnDodo');const btnVitoria=document.getElementById('btnVitoria');const tipoSelect=document.getElementById('tipo');const parcelasGroup=document.getElementById('parcelasGroup');const parcelasInput=document.getElementById('parcelas');const descricaoInput=document.getElementById('descricao');const valorInput=document.getElementById('valor');let isSubmitting=false;function formatarValor(v){let l=v.replace(/[^\d,]/g,'');if(l.includes(',')){const[i,d]=l.split(',');l=`${i},${(d+'00').slice(0,2)}`;}else if(l){l=`${l},00`;}return l?`R$ ${l}`:'';}function parseValorParaApi(v){let l=v.replace(/[R$\s.]/g,'');l=l.replace(',','.');return parseFloat(l)||0;}function validarObrigatorio(input,errEl){if(!input.value.trim()){errEl.textContent='Campo obrigatório';input.classList.add('error');return false;}errEl.textContent='';input.classList.remove('error');return true;}function validarValor(input,errEl){const v=input.value.trim();if(!v)return validarObrigatorio(input,errEl);const regex=/^R?\s*\d{1,3}(\.\d{3})*(,\d{2})?$|^\d+(,\d{2})?$/;if(!regex.test(v)){errEl.textContent='Formato inválido. Ex: R$ 100,00';input.classList.add('error');return false;}errEl.textContent='';input.classList.remove('error');return true;}function closeModal(){form.reset();statusMessage.classList.add('hidden');}btnClose.addEventListener('click',closeModal);btnCancel.addEventListener('click',closeModal);document.addEventListener('keydown',(e)=>{if(e.key==='Escape'){e.preventDefault();closeModal();}});function selecionarUsuario(uid,btn){usuarioInput.value=uid;[btnDodo,btnVitoria].forEach(b=>b.classList.remove('active'));btn.classList.add('active');}btnDodo.addEventListener('click',()=>selecionarUsuario(1,btnDodo));btnVitoria.addEventListener('click',()=>selecionarUsuario(2,btnVitoria));tipoSelect.addEventListener('change',()=>{if(tipoSelect.value==='parcelada'){parcelasGroup.classList.remove('hidden');parcelasInput.required=true;}else{parcelasGroup.classList.add('hidden');parcelasInput.required=false;parcelasInput.value='';}});valorInput.addEventListener('input',(e)=>{const cp=e.target.selectionStart;e.target.value=formatarValor(e.target.value);if(cp!==undefined)e.target.setSelectionRange(cp,cp);});descricaoInput.addEventListener('blur',()=>validarObrigatorio(descricaoInput,document.getElementById('error-descricao')));valorInput.addEventListener('blur',()=>validarValor(valorInput,document.getElementById('error-valor')));tipoSelect.addEventListener('blur',()=>validarObrigatorio(tipoSelect,document.getElementById('error-tipo')));form.addEventListener('submit',async(e)=>{e.preventDefault();if(isSubmitting)return;isSubmitting=true;btnSubmit.disabled=true;const errors=[];if(!validarObrigatorio(descricaoInput,document.getElementById('error-descricao')))errors.push('descrição');if(!validarValor(valorInput,document.getElementById('error-valor')))errors.push('valor');if(!validarObrigatorio(tipoSelect,document.getElementById('error-tipo')))errors.push('tipo');if(errors.length>0){showStatus(`Corrija: ${errors.join(', ')}`,'error');isSubmitting=false;btnSubmit.disabled=false;return;}loading.classList.remove('hidden');statusMessage.classList.add('hidden');const payload={usuario_id:parseInt(usuarioInput.value,10),descricao:descricaoInput.value.trim(),valor:parseValorParaApi(valorInput.value),tipo:tipoSelect.value,terceiro:document.getElementById('terceiro').value.trim()||null,parcelas:tipoSelect.value==='parcelada'?parcelasInput.value.trim():''};try{const result=await window.widgetAPI.submitLancamento(payload);if(result.success){showStatus('✅ Lançamento confirmado!','success');form.reset();setTimeout(()=>closeModal(),1500);}else{showStatus(`❌ ${result.error}`,'error');}}catch(err){console.error('[Form] Erro:',err);showStatus('❌ Erro de comunicação.','error');}finally{loading.classList.add('hidden');isSubmitting=false;btnSubmit.disabled=false;}});function showStatus(msg,type){statusMessage.textContent=msg;statusMessage.className=`status-message ${type}`;statusMessage.classList.remove('hidden');if(type==='success')setTimeout(()=>statusMessage.classList.add('hidden'),3000);}if(window.widgetAPI?.onFocusForm){window.widgetAPI.onFocusForm(()=>{descricaoInput.focus();descricaoInput.select();});}setTimeout(()=>descricaoInput.focus(),100);console.log('[Widget Form] Inicializado');})();