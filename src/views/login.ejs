<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Login - Gestão Financeira</title>
    <link rel="icon" type="image/x-icon" href="favicon.ico?v=1"/> 
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
</head>
<body class="login-body">

    <div class="login-container">
        <div class="card login-card">
            <h1 style="margin-bottom: 20px; font-size: 1.5rem;">Acesso Restrito</h1>
            
            <% if (error) { %>
                <div style="background: rgba(239, 83, 80, 0.1); color: var(--red); padding: 10px; border-radius: 8px; margin-bottom: 20px; font-size: 0.9rem; border: 1px solid var(--red);">
                    <%= error %>
                </div>
            <% } %>

            <form action="/login" method="POST">
                <div class="form-group">
                    <label>Senha Mestra</label>
                    <input type="password" name="password" class="form-input" placeholder="Digite a senha..." required autofocus>
                </div>
                <button type="submit" class="btn-primary form-btn">Entrar</button>
            </form>
        </div>
    </div>

</body>
</html>